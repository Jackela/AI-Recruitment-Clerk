// Jest setup file for app-gateway
// Add any global test setup here

import { jest } from '@jest/globals';

// Set test timeout
jest.setTimeout(30000);

// Global test setup
beforeAll(() => {
  // Setup code that runs before all tests
});

afterAll(() => {
  // Cleanup code that runs after all tests
});
